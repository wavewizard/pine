defmodule Pine.EventStore do
  @moduledoc """
   The Behaviour for Event Store 
  """

  @doc """
  Append Event
  """
  # @callback append(stream, event)
  # @callback read_stream_forward(stream_id)
  # @callback get_count(stream_id)
end
